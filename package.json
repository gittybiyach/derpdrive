{
  "name": "derpdrive",
  "version": "1.0.0",
  "private": true,
  "description": "Google Drive MCP server with full CRUD. Google's version is a pussy.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "auth": "node dist/index.js --auth"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.7.0",
    "googleapis": "^144.0.0",
    "open": "^10.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
